-- PROFILE
-- This is the initialization file for the p9 configuration.

require("p9.globals") -- Imports custom global options (vim.g...)
require("p9.options") -- Imports custom options (vim.opt...)
require("p9.remaps") -- Imports custom key / motion remaps (vim.keymap.set...)
require("p9.lazy_manager") -- Imports the Lazy package manager 
require("p9.plugins") -- Imports the plugin set up files for the lazy package manager
